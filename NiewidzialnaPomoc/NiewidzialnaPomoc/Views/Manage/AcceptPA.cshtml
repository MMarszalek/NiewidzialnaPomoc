@model Repository.Models.Advertisement

@{
    ViewBag.Title = "Accept";
}

<h2>Accept</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <h4>Cieszymy się, że udało Ci się znaleźć pomoc! Możesz się teraz odwdzięczyć osobom, które wyciągnęły do Ciebie rękę.</h4>

        <div class="form-group">
            @Html.LabelFor(model => model.Helpers, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @* TODO *@  
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.PerformanceId, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("PerformanceId", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.PerformanceId, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
